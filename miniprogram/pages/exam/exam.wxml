<question isEnd="{{end}}" wx:for="{{qs}}" questionInfo="{{item}}" wx:for-index="index" bindChoice="choice" id="{{index}}"></question>
<button class="submit" bindtap="submit" wx:if="{{!end}}">提交</button>
<button class="submit" bindtap="addWrong" wx:if="{{end&&!addR}}">收录所有错题</button>